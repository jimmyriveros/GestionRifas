# PROBLEMAS CONOCIDOS Y RIESGOS

**Actualizado:** 2026-08-09 (auditoría de contexto y continuidad posterior a la Fase 9). Este
documento **no oculta errores**.
Las trampas más frecuentes están resumidas en [`HANDOFF.md`](HANDOFF.md) §9.
Los hallazgos de la auditoría final, con su evidencia, están en
[`AUDIT_REPORT.md`](AUDIT_REPORT.md).

---

## 1. Problemas

`Abierto` = requiere acción · `Mitigado` = hay que seguir un procedimiento · `Info` = no es un defecto

| ID | Problema | Estado | Qué hacer |
|---|---|---|---|
| I-001 | Supabase CLI no instalada globalmente | ✅ Resuelto (F1) | Se usa como devDependency vía `npx supabase` |
| I-002 | Docker no disponible | ✅ Resuelto (F2) | Instalado. Necesario para BD local, `test:db` y `gen types` |
| I-003 | Sin credenciales de Supabase | ✅ Resuelto (F1) | Proyecto real configurado en `.env.local` |
| I-004 | `CLAUDE.md` y `CLAUDE.md.txt` coexisten | ✅ Resuelto (2026-08-05) | `CLAUDE.md` era el canónico (D-003) y el `.txt` una copia congelada del prompt maestro, ya sin §35. El usuario autorizó borrarlo y se eliminó. Queda `PROMPT FASE 0.txt`, que es el registro histórico del arranque del proyecto, no una fuente de reglas |
| I-005 | `db.<ref>.supabase.co` no resuelve por DNS | Mitigado | Supabase solo lo expone por IPv6 sin el add-on de IPv4. **Usar el session pooler**: `aws-0-<región>.pooler.supabase.com:5432`, usuario `postgres.<ref>`, contraseña percent-encoded |
| I-006 | `gen types --db-url` exige Docker | ✅ Resuelto (F2) | Se genera con `--local`. Cierra D-034 y DT-10 |
| I-007 | `auth.admin.createUser({password})` no deja la contraseña usable de inmediato | Mitigado | Verificado: el primer `signInWithPassword` falla; un `updateUserById` posterior con la misma contraseña sí funciona. `scripts/seed.ts` ya lo aplica. **Ya no afecta al alta de usuarios**: la Fase 3 invita por correo y nunca crea contraseñas (D-045). Sigue vigente solo para el seed |
| I-008 | `invalid_credentials` con la contraseña correcta | Info | Límite de intentos de Supabase Auth tras varios fallos seguidos. Esperable en desarrollo, no en operación normal |
| I-009 | Clic en viewport móvil no automatizable con la herramienta de este entorno | Info | La estructura a 375px se verificó por árbol de accesibilidad. Validar en dispositivo real o con Playwright (F7) |
| I-010 | Un `\r` dentro de un valor de `.env.local` | ✅ Resuelto (F2) | Se generó con `openssl … > archivo` en Git Bash sobre Windows; `tr -d '\n'` dejó el retorno de carro. Efecto: contraseña imposible de teclear, login OK por API y fallido en navegador. **Nunca construir valores de `.env` con redirecciones de shell en Windows** |
| I-011 | Al desactivar a un usuario, desaparecía del listado y era imposible reactivarlo | ✅ Resuelto (F3) | La política `profiles_select` de 0001 exigía `m_target.is_active`: sin perfil visible, `listOrgMembers` descartaba la fila. Corregido por la migración **`0011_profiles_visible_when_inactive.sql`**, aplicada en local **y en el proyecto real**. Lo detectó una prueba end-to-end, no una revisión de código |
| I-012 | La vista previa del navegador integrado no revela el contenido en Suspense | Info | React revela los límites de Suspense con `requestAnimationFrame`; si el panel no está visible, no compone fotogramas y el esqueleto se queda fijo. El servidor sí devuelve el HTML completo (200). No afecta a usuarios reales ni a Playwright |
| I-013 | `.env.local` apunta al proyecto **real**, no al local | Mitigado | `npm run dev` usaría producción para desarrollar. Usar `npm run dev:local` (D-047) para desarrollar y para las pruebas E2E |
| I-015 | El historial **ocultaba** los pagos que registraba un administrador para el cliente de un vendedor | ✅ Resuelto (F5) | `v_payment_history` unía `profiles` con INNER JOIN para los nombres. Como la vista es `security_invoker` y un vendedor no ve el perfil del administrador, el JOIN eliminaba la fila **entera**: el pago existía en `payments` (y su política se lo permitía) pero no en su historial. Corregido por la migración **`0012`** con LEFT JOIN. Detectado por un sondeo previo a escribir la interfaz, no por una prueba |
| I-016 | `MoneyInput` **concatenaba** los dígitos al escribir sobre él de forma programática | ✅ Resuelto (F5) | El componente reescribía el contenido del input al enfocarlo (estado `raw`/`focused`), así que un `fill()` podía añadir en vez de reemplazar: «50000» + «30000» = «5000030000». Corregido derivando lo mostrado solo de `value` (D-053). Afectaba también a gestores de contraseñas y autocompletado móvil |
| I-014 | `notFound()` responde **200**, no 404, en segmentos con `loading.tsx` | Info | La respuesta ya iba en streaming cuando se resolvió `notFound()`, así que el código de estado ya estaba enviado. **No es una fuga**: la página muestra «Pagina no encontrada» y no revela ningún dato del recurso ajeno, y así lo comprueban las pruebas E2E de aislamiento. Afecta al SEO de rutas públicas, que aquí no existen |
| I-017 | Toda fecha de **día calendario** se mostraba **un día antes** | ✅ Resuelto (F6) | `payment_date`, `sale_date`, `start_date` y `end_date` son columnas `date`: PostgREST las entrega como `'AAAA-MM-DD'` y `new Date('2026-08-04')` es **medianoche UTC**, que en Bogotá (UTC-5) todavía es el día 3. Afectaba a la fecha de todo abono, toda venta y toda rifa en pantalla. Corregido en `src/lib/dates.ts`: las cadenas de solo fecha se anclan al mediodía UTC antes de formatearlas, con lo que se arreglan de golpe los 8 sitios que las mostraban. Regresión cubierta en `tests/unit/dates.test.ts` y en E2E |
| I-018 | El helper `logout()` de las pruebas E2E nunca funcionó | ✅ Resuelto (F6) | Buscaba un botón llamado `/menu de usuario\|cuenta/i`, pero el disparador del menú no tenía nombre accesible: su contenido eran las iniciales del avatar y un nombre oculto bajo `md`. Era código muerto —ninguna prueba lo usaba— hasta que la Fase 6 necesitó cambiar de usuario dentro de una prueba. Corregido añadiendo `aria-label="Menu de usuario: <nombre>"` al disparador, que además **arregla un defecto real de accesibilidad**: en un teléfono, un lector de pantalla anunciaba solo «CR» |
| I-019 | **Toda consulta con RLS llamaba a una función por fila**: ~1,7 s con 7.278 boletas | ✅ Resuelto (F7) | Lo destapó el `EXPLAIN ANALYZE` de la revisión de rendimiento. `is_org_staff(organization_id)` recibe una **columna**, así que el planificador no puede sacarla del bucle: una llamada por fila, y cada una consulta tres tablas (44.367 accesos a buffer para 566 páginas). No se ve en desarrollo, donde el seed tiene 30 boletas, y **empeora con los datos** porque el coste es por fila. Corregido por la migración **`0014`**: `columna in (select current_staff_org_ids())` se evalúa una sola vez. Medido: **1.667 ms → 1,18 ms**. Sin cambio de permisos (D-063) |
| I-020 | En el **proyecto real**, `anon` podía ejecutar **todas** las funciones de `public`, incluidas las 6 RPC de negocio | ✅ Resuelto (F7) | Apareció al verificar el catálogo **después** de aplicar `0012`–`0014`. Misma causa que D-038, ahora con funciones: Supabase concede por `ALTER DEFAULT PRIVILEGES` **directo al rol**, y `revoke execute … from public` no deshace eso. **No hubo fuga**: `anon` no tiene privilegios de tabla (`0010`) y toda RPC empieza por `require_auth()`, así que obtenía `permission denied for table payments` o «Debes iniciar sesion». Pero la invariante que `catalog.test.ts` daba por cierta era **falsa en producción**. Corregido por `0015` (revoca de `anon` **y** de `public`), más `npm run verify:remote` para que la divergencia no vuelva a esconderse |
| I-021 | Las cuentas de demostración (`owner@demo.test`, etc.) conviven en el mismo proyecto Supabase que se usa como producción (F8, D-066), con una contraseña **compartida y conocida** | Abierto | No es una fuga por sí sola (nadie externo conoce `SEED_DEFAULT_PASSWORD`), pero es una superficie innecesaria en cuanto haya datos reales. Recomendación operativa en `docs/OPERATIONS.md` §5: desactivarlas o rotarles la contraseña antes de operar con dinero o clientes reales. No se hizo automáticamente: es una decisión del dueño del negocio |
| I-022 | No existe un entorno de staging real: los Preview de Vercel, si llegaran a tener las variables de Supabase configuradas, escribirían sobre la misma base que producción | Mitigado | Consecuencia directa de D-066. Mitigación actual: las tres variables de Supabase solo están puestas en el scope **Production** del proyecto Vercel, nunca en Preview (`docs/DEPLOYMENT.md` §1 y §3.1). Si algún día se necesita Preview con datos, hace falta aprovisionar un segundo proyecto Supabase primero |
| I-023 | Un enlace de invitación o de recuperación de contraseña puede aterrizar en la portada en vez de en `/reset-password`, sin ningún error visible en el servidor | Mitigado; confirmar configuración | GoTrue solo respeta el `redirect_to` si coincide con Authentication → URL Configuration; si no, cae en silencio al `site_url` base. Se reprodujo en local en Fase 8. `PHASE_STATUS` registra configurada la URL base `https://gestion-rifas.vercel.app`; el comodín `/**` es la configuración deseada que esta auditoría pide confirmar. Una versión anterior de `DEPLOYMENT.md` mostraba además un alias largo distinto. Confirmar canónico y comodín antes de enviar una invitación; detalle en `DEPLOYMENT.md` §2.1 |
| I-024 | El proyecto real está en el plan **Free** de Supabase: sin scheduled backups, sin Point-in-Time Recovery, sin restore-to-new-project | Abierto | Confirmado en el dashboard (Database → Backups, 2026-08-04) por el usuario. No existe ningún backup restaurable desde el dashboard hoy. Mitigación de la Fase 8: respaldo lógico manual con `supabase db dump` (D-070), verificado localmente. **No es una solución permanente**: antes de operar con dinero o clientes reales hace falta actualizar a Pro o automatizar el respaldo manual desde fuera de Supabase (`docs/RUNBOOK.md` §5.3). Un intento inicial de generar el respaldo, sin restringir el esquema, volcó `auth.users` completo (contraseñas cifradas y tokens); se detectó al restaurar en local, se descartó de inmediato y no salió de la máquina — el procedimiento corregido (`--schema public` en el volcado de datos) ya no incluye nada de `auth` |
| I-025 | **Una organización podía quedarse sin ningún Owner activo, de forma irrecuperable desde la aplicación** | ✅ Resuelto (F9), **aplicado también en el proyecto real** (2026-08-05) | `memberships_one_owner_per_org` (0001) garantiza «como máximo un Owner», nunca «al menos uno». La política `memberships_update_staff` deja a un Owner degradarse a `seller` o desactivarse a sí mismo con una llamada directa a PostgREST, porque el rol **resultante** ya no es `owner`. Reproducido en local con la clave pública y la sesión real del Owner: **1 fila afectada**, y después quedan **0 Owners activos**. El estado no se repara desde la aplicación —el ex-Owner ya no es staff (0 filas) y un Admin no puede ascender a nadie a Owner (`42501`, BR-U03)—: hace falta un script con `service_role`. **No es escalada de privilegios**: exige las credenciales del propio Owner, la interfaz no expone el cambio de rol y nadie gana permisos, el Owner los pierde. Corregido por la migración **`0016`** (constraint trigger diferido, D-071). **Aplicada al proyecto real el 2026-08-05** con autorización explícita del usuario, tras respaldo lógico previo. Verificada allí de dos formas: estructuralmente (9 comprobaciones de catálogo, incluida la de que `anon` y `public` no pueden ejecutar la función — la clase de divergencia de D-038 e I-020) y **por comportamiento**: degradar al Owner en producción dentro de una transacción con `SET CONSTRAINTS ALL IMMEDIATE` dispara el error y se revierte, dejando el estado intacto. Ver `AUDIT_REPORT.md` A-02 |
| I-026 | La red estructural de la prueba 25 no cubría **6 de las 28** Server Actions | ✅ Resuelto (F9) | `tests/unit/server-actions-guard.test.ts` recorría `src/features/<módulo>/actions.ts` a **un solo nivel**, así que `tickets/assign/`, `tickets/bulk/` y `tickets/seller/` nunca se analizaban — justo las acciones que asignan boletas y las crean en lote. **No hubo vulnerabilidad**: las 6 tienen su `authorizeAction` correcto. Lo que faltaba era la red que impide olvidarla mañana. Corregido con recorrido recursivo, mínimo elevado de 15 a 28 y una prueba que compara la lista analizada contra el listado real de archivos. Comprobado inyectando temporalmente una acción sin guarda en un módulo anidado: antes pasaba inadvertida, ahora **falla**. Ver `AUDIT_REPORT.md` A-01 |
| I-027 | El seed deja a `vendedor2` **sin ningún pago**, lo que debilita las pruebas de aislamiento de cobranza | ✅ Resuelto (F9) | Como los 36 pagos de «Rifas Demo» son de `vendedor1`, su total coincide con el de la organización entera: la aserción `report_payment_totals(v1) == sum(payments where seller=v1)` **no distingue** «filtrado por vendedor» de «sin filtrar». La cobertura no era nula (la prueba complementaria «un vendedor sin pagos obtiene ceros» sí fallaría si la RLS se rompiera), pero faltaba la dirección fuerte. Verificado a mano en la auditoría con pagos en ambos vendedores: el aislamiento **se cumple** en `payments`, `payment_allocations`, `v_payment_history` y `report_payment_totals`. Cubierto ahora por `F9-02`. **Nota para quien toque el seed:** `F6-04` y `F9-02` dependen de este equilibrio; `F9-02` borra su pago de prueba con la conexión de superusuario para dejar el seed exacto. Ver `AUDIT_REPORT.md` A-03 |
| I-031 | Las pruebas E2E del recorrido guiado quedaron un rato sin ejecutar porque **Docker Desktop se cayó a mitad de la sesión** | ✅ Resuelto (2026-08-05) | Al volver Docker se ejecutaron: **152 E2E en verde** (142 anteriores + 10 del recorrido), en escritorio y en Pixel 7. Destaparon dos defectos reales, ya corregidos: (a) Radix marca su contenido como `dialog` y la tarjeta también, así que había **dos diálogos anidados** y un lector de pantalla habría anunciado uno vacío —el envoltorio pasa a `role="presentation"`—; (b) `next()` llamaba a `close()` **dentro del actualizador de `setIndex`**, que React ejecuta durante el render: avisaba «Cannot update a component while rendering a different component» al terminar el recorrido |
| I-032 | Una corrida completa de E2E falló en dos pruebas que no volvieron a fallar | ✅ **Causa encontrada y corregida** (2026-08-06) para `tour-responsive.spec.ts:29` | **Reproducido** al certificar el trabajo del 2026-08-06: falla una de cada pocas corridas completas, y pasa siempre al ejecutarla sola. No era «tiempos» sino una carrera concreta: `boundingBox()` **no auto-espera**, y la tarjeta del recorrido se reemplaza al cambiar de paso, así que entre el `toBeVisible()` y la medida cabe un instante en el que devuelve `null`. Corregido reintentando la medida con `expect.poll` (`dialogBox`). Verificado: 4 corridas seguidas de la spec y una suite completa de 161, todas en verde. **Cómo se vio la primera vez** (2026-08-05): en la primera corrida tras reiniciar Docker fallaron esta y `payments.spec.ts:210`, ambas se dieron por «tiempos» y no se reprodujeron entonces. Lo de `payments.spec.ts:210` sigue sin volver a verse; queda anotado, porque un fallo intermitente documentado es información y uno omitido es una trampa |
| I-030 | **Persisten mensajes de base de datos sin tildes** | Abierto | Varias definiciones vigentes con `raise exception` en `0004`, `0007`, `0016`, `0019` y `0020` contienen textos como «La rifa esta anulada» o «Debes iniciar sesion». `mapPgError` puede mostrarlos al usuario (D-044). No se da un total porque migraciones posteriores redefinen funciones. **No se corrigen editando migraciones aplicadas**: requieren una migración nueva posterior a `0020` y promoción segura con respaldo (D-070). Es redacción, no cálculo ni autorización (D-073) |
| I-029 | **Buena parte de los textos visibles está escrita sin tildes ni «ñ»** y ninguno se ha auditado contra la nueva guía de redacción | ✅ Resuelto (2026-08-05) en la capa de aplicación | «Pendiente de aprobacion», «Dueno», «telefono», «Todavia no tienes clientes»: **35 de 127** archivos `.tsx` contienen palabras sin acentuar, incluidas las etiquetas de estado de `src/lib/constants.ts`, que son la fuente única y aparecen en toda la aplicación. No es un defecto funcional —nada se rompe— pero contradice `docs/UX_COPY_GUIDELINES.md` §1 y resta credibilidad frente a un usuario real. Aparecido al crear la guía (D-072), no antes: hasta entonces no existía una norma que lo prohibiera. **Corregido el mismo día** con la autorización del usuario: 302 correcciones en 89 archivos de `src/`, 18 archivos de pruebas ajustados y las cuatro suites reejecutadas (D-073). Durante la corrección, el script tocó por error dos **identificadores** de código (`numeros` en `reports.spec.ts`, `ultimo` en `reports-responsive.spec.ts`) al confundir dos líneas de código con prosa; los detectó `tsc` y se revirtieron. Lo que **no** entra aquí son los mensajes de la base de datos: I-030 |
| I-028 | El seed falla con `AuthRetryableFetchError` (502) si se lanza inmediatamente después de `db:reset` | Info | `supabase db reset` reinicia los contenedores y GoTrue tarda unos segundos más que Postgres en aceptar peticiones. No es un defecto del proyecto. Esperar a que `curl http://127.0.0.1:54321/auth/v1/health` devuelva 200 antes de sembrar, o simplemente reintentar: el seed es idempotente |
| I-033 | **El cliente elegido al asignar una boleta se volvía ilegible al pasar el cursor por encima** | ✅ Resuelto (2026-08-06) | Reportado por el usuario con capturas. `hover:bg-accent` y `bg-primary text-primary-foreground` convivían en la misma lista de clases del `<button role="option">`: `:hover` añade una pseudoclase y gana al fondo de la selección, pero el color del texto no tiene rival y se queda. Quedaba **texto claro sobre fondo claro** — contraste medido **1,01** sobre un mínimo de 4,5, es decir invisible. Afectaba al nombre y al teléfono del cliente que el vendedor acababa de elegir, justo antes de confirmar una venta. Corregido escribiendo los estados como ramas excluyentes en un componente compartido, `OptionList` (D-077). Cubierto por dos pruebas E2E que miden el contraste **calculado por el navegador**; comprobadas al revés restaurando el CSS defectuoso |
| I-036 | **Un vendedor con más de 200 clientes no podía encontrar a los últimos para venderles ni cobrarles** | ✅ Resuelto (2026-08-06) | Los dos selectores de cliente —«Asignar a un cliente» y el de registrar un abono— precargaban como máximo 200 registros y filtraban **en memoria**. A partir del cliente 201 no había forma de encontrarlo por mucho que se escribiera su nombre, y no aparecía ningún aviso de que faltaran: la lista simplemente no lo tenía. No es un problema de rendimiento sino de corrección, y bloqueaba las dos operaciones que dan dinero. Corregido pasando la búsqueda al servidor con una Server Action (D-078): la consulta va contra toda la cartera que la RLS permite ver, sin techo. Cubierto por una prueba E2E que crea relleno suficiente para dejar al cliente buscado **fuera** del bloque inicial y comprueba primero que no está y después que la búsqueda lo encuentra |
| I-037 | El desplegable «Cliente» de los filtros de boletas sigue topado en 200 | Abierto | Es una lista fija **sin buscador**, así que su tope es un límite real: con más de 200 clientes, los últimos no se pueden usar como filtro. Es anterior a la búsqueda híbrida y se deja intacto a propósito (`CLIENT_FILTER_OPTIONS_LIMIT`), para no recortar de 200 a 50 al mover los selectores con buscador. La solución natural es convertirlo en un selector con búsqueda, como los otros dos; queda para cuando el volumen lo pida |
| I-039 | **Un teléfono guardado sin indicativo no se encontraba al buscarlo con «+57»** | ✅ Resuelto (2026-08-07) | Reproducido **contra producción** al verificar la búsqueda desde el camino de datos de la app, no en local. El teléfono de un cliente real está guardado como `3101112233`; al buscarlo como `+57 (310) 111-2233` la normalización producía `573101112233` —con indicativo— y eso no es subcadena de lo guardado. El fallo era **asimétrico**: al revés sí funcionaba, porque un término corto sí es subcadena del guardado largo. Y por eso se colo: la prueba E2E original guardaba el teléfono CON separadores y lo buscaba en dígitos, justo la dirección que ya andaba, de modo que la afirmación «encuentra el teléfono con cualquier formato» de D-079 y del commit era **más fuerte de lo que se había probado**. Corregido en `searchNeedle`: un término telefónico se reduce a su número **nacional** (los últimos 10 dígitos), que es subcadena de las dos formas de guardado. **Sin migración**: el arreglo es solo del término. Cubierto ahora en las dos direcciones —dos pruebas unitarias y dos E2E parametrizadas por forma de guardado—, comprobadas al revés rompiendo la corrección a propósito |
| I-038 | Fallos intermitentes en el bloque «Anulación de pagos» y en «pagos recientes», con `net::ERR_ABORTED at /login` | ✅ **Causa encontrada y corregida** (2026-08-06) | No era una aserción sobre pagos: era **`loginAs` perdiendo una carrera de navegación**. Tras `clearCookies()`, las peticiones RSC que quedaban en vuelo se encuentran sin sesión, el navegador redirige al login por su cuenta, y esa redirección aborta el `page.goto('/login')` del helper. La página de login aparece pintada en la captura del fallo, que es lo que delató que el producto estaba bien. Depende del tiempo: la corrida que pasó tardó 7,9 min y la que falló, 10,5. **Aislado con tres experimentos**: la suite completa sin `busqueda-hibrida.spec.ts` daba 161/161; con ella fallaba 2 de cada 4; y `payments.spec.ts` a solas falla en su **segunda** corrida sin que la búsqueda intervenga. Corregido en `fixtures.ts`: `gotoLogin` reintenta una vez ante `ERR_ABORTED`, porque la segunda navegación ya no compite con nada. Se redujo además la huella de la suite nueva (el relleno pasa de 60 inserciones a una, y borra también sus boletas). Los fallos de `reports.spec.ts` eran arrastre de estos |
| I-035 | `seller-tickets.spec.ts` (BR-I08) falla si la suite se corre varias veces sin `db:reset` | ✅ Mitigado (2026-08-06) | Esa prueba elige un cliente **sin escribir en el buscador**, y el selector muestra solo los **primeros 50** cuando el filtro está vacío. Cada ejecución que deja clientes nuevos acerca el límite; con ~50 el cliente recién creado ya no aparece y la prueba agota el tiempo esperando su opción. Apareció al desarrollar `filas-seleccionables.spec.ts`, que corría muchas veces seguidas creando 4 clientes cada vez. **No es un defecto del producto** —con el seed limpio la suite pasa 161/161— pero sí una fragilidad real de la prueba. Mitigado: la suite nueva crea **un** cliente para sus cuatro pruebas y lo borra en `afterAll` con la conexión de servicio, igual que `F9-02` con su pago. La fragilidad de fondo sigue ahí: cualquier prueba futura que cree clientes debe borrarlos |
| I-034 | Medir color en las pruebas leyendo `getComputedStyle` daba contrastes falsos de 1,00 | Info (trampa) | Con Tailwind 4 la paleta está en `oklch` y **el navegador devuelve el color ya calculado en `lab()` / `oklab()`**, no en `rgb()`. Interpretar sus tres números como canales de 0 a 255 daba «texto negro sobre fondo negro» en textos perfectamente legibles, y una prueba de contraste que pasaba o fallaba por motivos equivocados. Solución en `filas-seleccionables.spec.ts`: **pintar** el color en un `canvas` de 1×1 sobre blanco y sobre negro y leer los píxeles — de las dos medidas salen el color y su alfa. Segunda trampa del mismo sitio: `transition-colors` hace que medir justo después de un `hover()` capture un fotograma intermedio; hay que esperar a que el valor deje de cambiar |
| I-040 | **La migración `0018` está aplicada solo en local; el proyecto real todavía no la tiene** | ✅ Resuelto (2026-08-08) | Introduce `search_tickets` y los dos índices de trigramas de BR-N11. El código de la aplicación **ya llama a la función** cuando hay término de búsqueda: si se despliega el frontend sin aplicar la migración, buscar una boleta falla en producción (el listado sin buscar sigue intacto). Aplicarla exige respaldo lógico previo y autorización explícita del usuario, como la `0016` y la `0017` (`HANDOFF.md` §3 y §3.b). Aplicada al proyecto real el 2026-08-08 con autorización explícita, tras respaldo lógico previo (`Rifas-backups/2026-08-08-antes-0018/`, 0 referencias a `auth`), y verificada allí por catálogo (13/13) y **por comportamiento** contra los datos reales: encuentra por los dos números, no encuentra por código interno ni por texto, el diario va primero, y `anon` recibe `42501` por PostgREST —lo que además prueba que la caché de esquema se recargó y la función está expuesta— |
| I-041 | Los trigramas sobre `daily_number` y `weekly_number` no ayudan con términos de **dos** cifras | Info | `like '%00%'` no permite extraer ningún trigrama completo, así que PostgreSQL vuelve al barrido secuencial. Con 7.278 boletas son 1,2 ms y 165 páginas, frente a 58 páginas con tres cifras. Como el mínimo para que la búsqueda salga sola son dos caracteres, una parte de las búsquedas seguirá recorriendo la tabla. Se acepta a sabiendas: los números tienen 4 cifras como máximo y la alternativa —indexar sufijos— es desproporcionada para el tamaño de esta tabla. Revisar si el barrido empieza a notarse con cientos de miles de boletas |
| I-042 | **La migración `0019` está aplicada solo en local; el proyecto real todavía no la tiene** | ✅ Resuelto (2026-08-08) | Añade `taken_ticket_combinations` y `log_ticket_import` (BR-N12). El importador **ya las llama**: sin la migración, en producción la vista previa no podría avisar de combinaciones ya tomadas y la importación no quedaría registrada en la bitácora. Las boletas se crearían igual —eso va por `bulk_create_tickets`, que ya existe— y la restricción única seguiría protegiendo, así que el fallo sería de aviso, no de integridad. Aplicada al proyecto real el 2026-08-08 con autorización explícita, tras respaldo lógico previo (`Rifas-backups/2026-08-08-antes-0019/`, 9 tablas de negocio y 0 referencias a `auth`). Verificada por catálogo y **por comportamiento**, simulando la sesión con `request.jwt.claims` dentro de una transacción revertida: producción quedó con 0 filas `ticket.import` de prueba |
| I-043 | **La migración `0020` está aplicada solo en local; el proyecto real todavía no la tiene** | ✅ Resuelto (2026-08-08) | Añade las cinco funciones de las acciones masivas (BR-B01..BR-B08) y, sobre todo, **reescribe `assign_ticket` y `cancel_ticket` para que deleguen** en los helpers nuevos. El código de la aplicación ya llama a las funciones `bulk_*`: **desplegar sin aplicarla rompe en producción la selección múltiple entera y también el cambio de vendedor individual**, que ahora pasa por `bulk_change_ticket_seller`. Asignar y anular una sola boleta seguirían funcionando (las funciones viejas siguen ahí). Aplicada al proyecto real el 2026-08-08 con autorización explícita, tras respaldo lógico previo (`Rifas-backups/2026-08-08-antes-0020/`, 9 tablas de negocio y 0 referencias a `auth`). Verificada por catálogo (`verify:remote` 13/13 más 20 comprobaciones propias de privilegios) y **por comportamiento**, con sesión simulada dentro de una transacción revertida: producción quedó con 95 boletas antes y después, y 0 filas de bitácora de la prueba |
| I-044 | La aprobación en lote no es «todo o nada» en la base de datos | Info | `approve_tickets` (Fase 3) salta por su cuenta las boletas que no están pendientes y devuelve cuántas aprobó; no se tocó al añadir las acciones masivas. La pantalla lo compensa: el botón solo se habilita cuando **todas** las seleccionadas se pueden aprobar, así que el resultado es el que la persona vio antes de confirmar. Si alguna vez hace falta el todo-o-nada real también ahí, es una migración de tres líneas |
| I-045 | Eliminar boletas no pide una autorización más fuerte que anular | Info | El encargo pedía reutilizar «el mismo mecanismo fuerte» de las acciones sensibles, y ese es el que hay: rol Dueño/Administrador, confirmación explícita y motivo obligatorio. **Este proyecto no tiene reautenticación por contraseña, PIN ni código**, así que no existe nada más fuerte que reutilizar; inventar uno solo para esta acción habría dejado una pieza suelta sin recuperación ni bloqueo por intentos. Queda anotado por si el negocio lo pide (D-084) |
| I-046 | Algunas lecturas auxiliares y perfiles pueden truncarse sin aviso por el límite de PostgREST | Abierto | Los agregados y CSV sí usan `fetchAllRows`, pero `listOrgMembers`, opciones de rifas, `listPayableTickets` y los perfiles de cliente conservan consultas sin paginación completa; estos últimos muestran hasta 100 boletas/pagos. A gran volumen pueden faltar opciones, boletas cobrables o historial sin que la pantalla lo indique. `findExistingCombinations` también puede recibir una respuesta parcial en la vista previa; la restricción única final sigue protegiendo la integridad. Paginar o usar consultas/RPC acotadas antes de superar esos volúmenes |
| I-047 | No existe un contexto explícito de organización activa para una persona con varias membresías | Abierto antes de habilitar ese escenario | La base permite varias membresías activas en organizaciones distintas y RLS autoriza todas mediante `current_org_ids()`, pero `getActiveMembership()` elige la primera para resolver rol y rutas. No crear usuarios multi-organización reales hasta definir una organización activa firmada y hacer que sesión, consultas y RLS compartan ese alcance. No afecta al uso actual de una membresía activa por persona |
| I-048 | El preflight local conserva deriva de configuración y comentarios | Mitigado | `supabase/config.toml` referencia `supabase/seed.sql`, que no existe, por lo que `db:reset` muestra una advertencia; el seed real e idempotente es `scripts/seed.ts` y se ejecuta con `npm run seed:local`. `.env.example` y comentarios del CI también conservan nombres o conteos históricos. Los comandos efectivos funcionan; no se cambió configuración en esta auditoría documental |
| I-049 | `NEXT_PUBLIC_SITE_URL` se documenta como obligatoria pero `check-env` no la valida | Abierto | Invitaciones y recuperación construyen el callback con esa variable y usan cadena vacía si falta. `npm run build` puede quedar verde con enlaces incorrectos. Añadirla a la validación solo en una tarea funcional/configuración autorizada; hasta entonces comprobarla manualmente en Vercel según `DEPLOYMENT.md` §3.1 |
| I-050 | Persisten textos visibles sin tildes después de D-073 | Abierto | Ejemplos confirmados: «quedo registrado» en `ClientForm`, «La rifa quedo» en `RaffleStatusActions`, «cantidad maxima» en el esquema de carga masiva y «acceso a el» en `lib/errors.ts`. No afecta autorización ni datos, pero contradice `UX_COPY_GUIDELINES.md`; corregir con su checklist y ajustar pruebas en una tarea de interfaz autorizada |
| I-051 | La validación de entradas del servidor no es uniforme en consultas auxiliares y filtros URL | Abierto | `findExistingCombinations` es la única Server Action parametrizada que no recibe `unknown` ni valida con Zod. Varias listas pasan UUID/fechas de la URL directamente a PostgREST; una URL manipulada puede terminar en error de consulta en vez de una respuesta controlada. RLS, RPC y restricciones siguen siendo la frontera de seguridad, pero falta endurecer la capa de servidor |
| I-052 | `npm run format:check` no está en verde sobre el árbol funcional actual | Abierto, no bloqueante | Prettier reporta 60 archivos de código/pruebas existentes; ninguno de los documentos modificados por esta auditoría aparece en la lista. `verify` no incluye esta comprobación y sigue siendo la puerta de calidad actual. No se reformatearon archivos fuera de alcance: hacerlo requiere una tarea mecánica separada para no mezclar un diff masivo con trabajo funcional |
| I-053 | Next.js actualiza su bloque de reglas solo en `AGENTS.md` cuando también existe `CLAUDE.md` | Mitigado con protocolo manual | El generador de Next prioriza `AGENTS.md`; por eso una actualización de la dependencia puede dejar el bloque duplicado de `CLAUDE.md` obsoleto. Tras actualizar Next se ejecuta `next dev` y se copia únicamente el bloque `BEGIN/END:nextjs-agent-rules` de `AGENTS.md` a `CLAUDE.md`. No se importa un archivo raíz completo dentro del otro, porque contienen instrucciones distintas |
| I-054 | **La migración `0021` estaba solo en local; desplegar el frontend sin ella rompía la importación con cliente** | ✅ Resuelto (2026-08-09) | Aplicada al proyecto real con autorización expresa tras respaldo lógico externo (`Rifas-backups/2026-08-09-antes-0021/`, 0 referencias a credenciales de Auth). El dry-run mostró únicamente `0021`; `db push` terminó correctamente; `verify:remote` pasó 13/13. Una sonda con sesión Owner comprobó catálogo, privilegios, índice, vista previa e importación de tres filas —dos al mismo cliente y una sin cliente— dentro de una transacción revertida, con 0 clientes y 0 boletas residuales |

| I-055 | **Una prueba de la importación afirmaba sobre la boleta equivocada y tumbaba el CI de vez en cuando** | ✅ Resuelto (2026-08-10) | `recordarBoletas()` de `tests/db/ticket-import.test.ts` consultaba filtrando **solo por `daily_number`**, y `numeros()` sortea los dígitos. Como un número diario puede repetirse en otra combinación (BR-N07), el `find()` posterior podía quedarse con una boleta de otra prueba —anulada, por ejemplo— en vez de la recién importada: `expected 'available', received 'cancelled'`. **No es un defecto del producto**: la RPC hacía lo correcto; la prueba miraba la fila que no era. Apareció en el CI del commit `bb6db5f`, que es **solo documentación**, mientras el commit funcional anterior había pasado 2/2 y la suite pasaba en local — la señal de que era aleatorio, no una regresión. Reproducido a propósito plantando `4242/1111` anulada y `4242/2222` disponible: la consulta devolvía las dos y `find()` elegía la anulada. Corregido acotando por la **combinación completa**, que es lo único único dentro de una rifa (BR-N04); de paso, la limpieza deja de poder borrar boletas de otras pruebas. Comprobado al revés —con los mismos datos, la lógica vieja elige la anulada y la nueva la correcta— y con cuatro corridas seguidas de `test:db` en 378/378. Misma familia que I-035 |

| I-056 | **El panel administrativo se desplaza en horizontal a 320 px** | ⚠️ Abierto (2026-08-13) | La tabla «Resumen por vendedor» de `/owner/dashboard` mide 547 px y no está dentro de un contenedor con `overflow-x`, así que a 320 px se desplaza la **página entera** — 45 px medidos. **Preexistente y ajeno a los equipos**: ese archivo no se tocó en ese trabajo, y su último cambio es de `45759f6`. No se había visto porque el proyecto E2E móvil usa Pixel 7 (412 px), donde la tabla cabe; apareció al probar 320 px por primera vez, en `equipo-movil.spec.ts`. El patrón correcto ya existe en el proyecto: los reportes envuelven sus tablas anchas para que el scroll ocurra **dentro** del contenedor y no en la página. Arreglo estimado: envolver esa tabla igual. No se hizo al encontrarlo por estar fuera del alcance autorizado |

| I-057 | **`test:db` fallaba de forma intermitente, incluso sobre una base recién sembrada** | ✅ Resuelto (2026-08-14) | **Causa real, confirmada por el log del CI**: `duplicate key value violates unique constraint "tickets_combo_unique"`. Dos suites de comisiones construían los números de sus boletas a partir de una **base de dos cifras sorteada** (`Math.random() * 90 + 10`), y solo hay noventa: `commission-modes` la sorteaba **en cada llamada sin recordar las anteriores** (seis llamadas ⇒ ~15% de colisión por ejecución) y `commissions` reservaba con ella un bloque de 60 boletas —su comentario decía «sin azar», y no lo era—. Al chocar, el `insert` lanzaba y arrastraba todo lo que venía detrás: en `commissions` reventaba el `beforeAll` y sus 19 casos salían como «skipped»; en `commission-modes` moría la prueba en curso y, si era antes de que E6-04 restaurase el precio, la rifa se quedaba en `$120.000` y hacía fallar la **ejecución siguiente** con `expected 60000 to be 50000` — un síntoma que apuntaba al sitio equivocado y que en un primer diagnóstico se atribuyó al precio compartido. **No lo introdujo `0026`**: el defecto vive en dos archivos que esa migración no toca, y se reprodujo con la suite nueva excluida. Lo destapó el CI de `8e88e81`, un commit **solo de documentación**. Corregido en las dos suites: los números se buscan hasta encontrar uno libre y se reintenta solo ante `23505`, nunca ante otro error. Comprobado con **10 pasadas completas seguidas** sobre la misma base, 457/457 todas. Misma familia que I-035 e I-055 |
| I-058 | **El CSV de vendedores sigue diciendo «Activo» donde la pantalla dice «Cuenta activa»** | ℹ️ Info (2026-08-14) | `sellerColumns` de `src/features/reports/export.ts` deriva la columna «Estado» de `is_active`, que es exactamente lo que dice, pero ya no coincide con la etiqueta de la interfaz (BR-E14). No es incorrecto ni afecta a ningún cálculo: para incluir «Invitación pendiente» habría que devolver `activated_at` desde las funciones de reporte, que es un cambio de SQL fuera del alcance de este trabajo |

**No hay trabajo técnico activo autorizado.** Para operar con datos o dinero reales deben resolverse
o aceptarse expresamente I-021, I-023 e I-024. I-030, I-037 e I-046–I-052 son trabajo de ingeniería
pendiente priorizable; I-053 queda mitigado por el protocolo de sincronía e I-054 está resuelto.
Ninguno de los asuntos abiertos se corrige con un cambio remoto sin autorización.

---

## 2. Riesgos técnicos

| ID | Riesgo | Mitigación | Estado |
|---|---|---|---|
| R-01 | Recursión de RLS al consultar `memberships` desde sus propias políticas | Funciones `SECURITY DEFINER STABLE` que omiten RLS | ✅ Verificado F2 |
| R-02 | Sobrepago por concurrencia | `FOR UPDATE` ordenado + snapshot nuevo por sentencia + `CHECK` | ✅ Probado con pagos concurrentes reales |
| R-03 | Vista sin `security_invoker` → fuga entre vendedores | Regla obligatoria + prueba de catálogo que falla sola | ✅ **Reverificado F9**: las 5 vistas, en local y en el proyecto real |
| R-04 | Función `SECURITY DEFINER` sin `search_path` | Obligatorio en las funciones propias + prueba de catálogo | ✅ **Reverificado F9**: las 25 funciones propias lo declaran; las de `pg_trgm` quedan fuera a propósito |
| R-05 | Navegador congelado con 1.000 filas | Virtualización (`@tanstack/react-virtual`), validación pura O(n), lotes de 100 | ✅ Verificado F3: 1.000 filas generadas y guardadas en ~5 s, <60 filas en el DOM |
| R-06 | Fuga de `SERVICE_ROLE_KEY` al navegador | `import 'server-only'` + prohibido `NEXT_PUBLIC_` en secretos | ✅ Verificado F1 |
| R-07 | Desfase de zona horaria en pagos nocturnos | Fechas de negocio como `date` con `today_bogota()`; `TZ=UTC` | ✅ Implementado F2 |
| R-08 | Recálculo de `paid_amount` que no cubra algún camino | Cubre INSERT/UPDATE/DELETE + cambio de `voided_at`; prueba que compara con la suma real | ✅ Verificado F2 |
| R-09 | El índice único no aplica con números `NULL` en `draft` | Es el comportamiento buscado; obligatorios al salir de `draft` (D-017) | ✅ Por diseño |
| R-10 | Consultas N+1 en listados | Vistas agregadas + `select` con relaciones; nombres de vendedor resueltos con **un** mapa en memoria | ✅ Reverificado F6 con 5.000 boletas: los reportes devuelven 1–2 filas agregadas, no 5.000 |
| R-18 | **Truncamiento silencioso de PostgREST a 1.000 filas** en agregados y exportaciones | `fetchAllRows` (bloques de 1.000 con orden estable) y `count: 'exact', head: true` en esos caminos; las lecturas auxiliares pendientes están en I-046 | ✅ Verificado F6 para agregados/CSV |
| R-19 | Una exportación que alcance el tope de 50.000 filas parecería completa | `fetchAllRows` devuelve `truncated`, y el CSV termina con una línea `AVISO;…` que dice que está incompleto | ✅ Implementado F6. No alcanzable con el volumen real de este negocio; existe para que nunca sea silencioso |
| R-11 | Errores de PostgreSQL expuestos al usuario | `mapPgError` traduce por código y por restricción, y propaga solo los mensajes de negocio propios (D-044) | ✅ Ampliado en F3 |
| R-12 | Deriva entre el esquema real y `DATA_MODEL.md` | Actualización obligatoria + tipos generados + pruebas de catálogo | 🔄 Permanente |
| R-13 | Sesión de usuario recién desactivado | `is_active` verificado en cada request y en `current_org_ids()` | ✅ Verificado F1/F2 |
| R-14 | Fallo parcial en carga masiva | RPC por lote que devuelve insertados y conflictos sin abortar; la interfaz conserva solo las filas rechazadas | ✅ Verificado F3 |
| R-15 | Bloqueo del contador al insertar 1.000 boletas | La RPC reserva el bloque completo de una vez | ✅ Implementado F2 |
| R-16 | `bigint` serializado a `number` en JS | Importes muy por debajo de 2^53 (D-008) | ✅ Aceptado |
| R-17 | Vercel y Supabase en regiones distintas | Elegir regiones cercanas | ⬜ Control en F8 |

---

## 3. Deuda técnica aceptada

| ID | Deuda | Razón | Revisión |
|---|---|---|---|
| DT-01 | TypeScript 5.9.3, no 7.x | `typescript-eslint@8` exige `<6.1.0` (D-002) | Cuando lo soporte |
| DT-02 | `paid_amount` desnormalizado | Permite el `CHECK` de sobrepago y filtrar sin agregar (D-009) | Se mantiene |
| DT-03 | `organization_id` repetido en todas las tablas | RLS simple y eficiente + FK compuestas (D-007) | Se mantiene |
| DT-04 | Métodos de pago no configurables | Innecesario para el MVP (D-020) | Si el negocio lo pide |
| DT-05 | Una sola organización real | El modelo ya soporta varias | Ninguna |
| DT-06 | Sin internacionalización | El negocio opera en Colombia | Fuera del MVP |
| DT-07 | `supabase-js@2.109` / `ssr@0.12.0`, no las últimas | 2.110+ exige Node ≥22 (D-029) | Al pasar a Node 22 |
| DT-08 | `jsdom@29`, no 30 | 30 exige Node ≥22.22 (D-030) | Junto con DT-07 |
| DT-09 | `eslint@9`, no 10 | El `eslint-plugin-react` interno de `eslint-config-next` no admite 10 (D-031) | Al actualizarse |
| ~~DT-10~~ | ~~`database.types.ts` a mano~~ | **Saldada en F2**: se genera con `gen types --local` | — |
| ~~DT-11~~ | ~~Playwright no instalado~~ | **Saldada en F3**: `@playwright/test` + Chromium instalados, 41 specs en `tests/e2e/` | — |
| DT-12 | ~~3 vulnerabilidades altas de `npm audit`~~ | ✅ **Saldada en la Fase 7.** En la Fase 2 el único «arreglo» que ofrecía npm era degradar Next a la versión de 2019, así que se aceptó como deuda. Al reevaluarla en la Fase 7 la situación había cambiado: el arreglo pasó a ser **subir** a `next@16.3.0`, dentro de la misma major | Hecho: Next y `eslint-config-next` a 16.3.0 (versiones fijas, sin `^`), `npm audit` en **0 vulnerabilidades**, y las tres suites en verde tras la subida |

---

## 4. Estado del proyecto Supabase real

**Las 21 migraciones `0001`–`0021` están aplicadas y verificadas** en local y producción
(última comprobación registrada: 2026-08-09, `npm run verify:remote` en **13/13** más sondas de
catálogo, privilegios y comportamiento para `0021`). Local y remoto vuelven a tener el mismo
conjunto de migraciones.

Comprobación reproducible en cualquier momento, de solo lectura:

```bash
npm run verify:remote
```

Ejecuta contra el proyecto real las mismas invariantes de catálogo que
`tests/db/catalog.test.ts` comprueba en local: RLS habilitada y forzada, `search_path` en las
funciones `SECURITY DEFINER`, `security_invoker` en las vistas, ausencia de políticas y privilegios
de `DELETE`, `anon` sin privilegios de tabla **ni de función**, dinero en `bigint` y ninguna política
llamando a una función por fila.

**Por qué existe este script (I-020).** Dos veces ya, una invariante era cierta en local y falsa en
el proyecto alojado, porque Supabase concede privilegios por `ALTER DEFAULT PRIVILEGES` y el
resultado depende del rol que aplique la migración:

| Cuándo | Qué divergía | Se corrigió con |
|---|---|---|
| Fase 2 (D-038) | `authenticated` conservaba `DELETE` en el remoto | `0010` |
| Fase 7 (I-020) | `anon` podía ejecutar **todas** las funciones | `0015` |

Las pruebas locales no podían detectarlo. Verificar el remoto es lo único que lo detecta.

Historial de aplicaciones al proyecto real:

| Fecha | Migraciones | Nota |
|---|---|---|
| 2026-08-03 | `0001`–`0010` | Alta inicial del esquema |
| 2026-08-03 | `0011` | Corrige I-011. Cambio de política de lectura: no alteró datos ni estructura |
| 2026-08-04 | `0012`, `0013`, `0014` | Historial de pagos, funciones de reporte y rendimiento de la RLS. `--dry-run` primero; las tres se aplicaron sin error |
| 2026-08-04 | `0015` | Cierra I-020, detectado al verificar el catálogo **después** de aplicar las tres anteriores |
| 2026-08-05 | `0016` | Cierra I-025 (una organización sin Owner). Respaldo lógico previo, `--dry-run` primero, y verificación posterior en dos niveles: catálogo (9/9) y **comportamiento** —el trigger rechaza degradar al Owner en producción, comprobado dentro de una transacción revertida— |
| 2026-08-07 | `0017` | Normaliza búsquedas; respaldo previo y verificación de catálogo y comportamiento |
| 2026-08-08 | `0018`, `0019`, `0020` | Búsqueda de boletas, importación y acciones masivas; cada promoción fue autorizada, respaldada y verificada |

La última verificación registrada de `npm run verify:remote` quedó en verde tras aplicar `0020`.

---

## 5. Fuera del MVP (no son fallas)

Pagos en línea · portal de clientes · integración con loterías · sorteos automáticos · números
ganadores · WhatsApp/SMS · facturación electrónica · comisiones · app nativa · contabilidad.

Reglas del MVP que podrían confundirse con defectos:
- Una combinación anulada **no** se reutiliza dentro de la misma rifa (BR-N08).
- La anulación de un pago es **irreversible**: se registra uno nuevo si hubo error (D-013).

---

## 6. Historial

| Fecha | Fase | Cambio |
|---|---|---|
| 2026-08-02 | 0 | 4 problemas, 17 riesgos, 6 deudas identificados. |
| 2026-08-03 | 1 | I-001 e I-003 resueltos. +I-005..I-009, +DT-07..DT-12. R-06 y R-13 verificados. |
| 2026-08-03 | 2 | I-002 e I-006 resueltos. +I-010. DT-10 saldada. R-01, R-02, R-03, R-04, R-08 verificados con pruebas automatizadas. |
| 2026-08-03 | 3 | +I-011 (resuelto con la migración 0011), +I-012, +I-013. DT-11 saldada. R-05, R-10, R-11, R-14 verificados. |
| 2026-08-03 | 4 | +I-014. R-13 reverificado con el portal del vendedor. Sin migraciones ni deuda nueva. |
| 2026-08-03 | 5 | +I-015 y +I-016, ambos resueltos en la misma fase (migración `0012` y `MoneyInput`). R-02 y R-16 reverificados con la interfaz de pagos. |
| 2026-08-04 | 6 | +I-017 y +I-018, ambos resueltos en la misma fase (fechas de día calendario y nombre accesible del menú de usuario). Migración `0013` **pendiente de aplicar al proyecto real**. I-011 verificado con una prueba explícita a 5.000 boletas. |
| 2026-08-04 | 7 | +I-019 (**RLS llamando a una función por fila**, ~1.400× más lenta), resuelto con la migración `0014`. DT-12 saldada subiendo Next a 16.3.0: `npm audit` en 0. Cabeceras de seguridad con CSP por nonce, limitación de intentos, y las 25 pruebas mínimas automatizadas por fin (la 1, la 2 y la 25 no lo estaban). R-03, R-04 y R-11 reverificados. |
| 2026-08-05 | 8 | +I-021..I-024 (cuentas de demostración en producción, sin staging, lista blanca de URL de Auth, y **plan Free sin backups automáticos**). DT-12 cerrada. |
| 2026-08-05 | — | Guía permanente de UX Writing (`docs/UX_COPY_GUIDELINES.md`, D-072) y aplicación de la guía a los textos existentes (D-073). +I-029 (**textos sin tildes**), **resuelto el mismo día** en la capa de aplicación: 301 correcciones en 88 archivos. I-004 cerrado (se borró `CLAUDE.md.txt`). +I-030 (**los mensajes de la base de datos siguen sin tildes**), separado a propósito porque exige una migración nueva sobre producción. Sin cambios de esquema. |
| 2026-08-06 | — | **Búsqueda híbrida** (D-078, D-079): pausa al escribir, `Enter` inmediato, limpiar y descarte de respuestas viejas en las cuatro listas y los dos selectores de cliente. +I-036 (**un vendedor con más de 200 clientes no podía encontrar a los últimos para venderles ni cobrarles**), **resuelto el mismo día**: era un fallo de corrección, no de rendimiento. +I-037 (el desplegable «Cliente» de los filtros sigue topado en 200, sin buscador) y +I-038 (`payments.spec.ts` no aguanta datos de una corrida anterior; reproducido con el código intacto). Migración `0017`: normalización de acentos y teléfonos en `clients`, e índice de trigramas en `tickets.internal_code`, que se buscaba sin ninguno — **aplicada también al proyecto real el 2026-08-07** y verificada allí por catálogo y por comportamiento |
| 2026-08-06 | — | **Usabilidad de tablas y listas** (D-076, D-077), a petición del usuario. +I-033 (**el cliente elegido se volvía ilegible al pasar el cursor**, contraste 1,01), **resuelto el mismo día**. +I-034 (medir color con `getComputedStyle` da contrastes falsos con la paleta `oklch`) e +I-035 (una prueba anterior depende de que el vendedor tenga menos de 50 clientes), ambos anotados como trampas. **I-032 cerrado**: el fallo intermitente del recorrido en móvil se reprodujo, resultó ser una carrera de `boundingBox()` y se corrigió. Sin migraciones ni cambios de esquema. +9 E2E, +15 unitarias |
| 2026-08-05 | 9 | Auditoría final: **47 intentos adversarios**, 45 bloqueados y 2 «no bloqueados» que resultaron ser comportamiento correcto. +I-025 (**una organización podía quedarse sin Owner**, migración `0016`, aplicada y verificada también en el proyecto real el mismo día, por comportamiento y no solo por catálogo), +I-026 (**6 de 28 Server Actions fuera de la red estructural**, corregido), +I-027 (seed asimétrico que debilitaba el aislamiento de cobranza, corregido con `F9-02`), +I-028 (Auth tarda en arrancar tras `db:reset`). R-03 y R-04 reverificados en local **y en producción** (`verify:remote`, 13/13). Informe completo en `AUDIT_REPORT.md`. |
| 2026-08-08 | — | **Una boleta se busca por sus números, no por su código** (BR-N11, D-080), a petición del usuario. El código interno sale de la búsqueda y de las listas y baja al detalle, bajo «Información administrativa». La búsqueda pasa a ser **parcial** y **ordenada por relevancia** —diario antes que semanal—, decidido en SQL por la función `search_tickets`. Migración `0018`: la función y dos índices de trigramas sobre los números. +I-040 (**la `0018` está solo en local y el código ya la llama: no desplegar sin aplicarla**) e +I-041 (los trigramas no ayudan con dos cifras). Corregido durante el trabajo un orden de resultados sin sentido para quien mira la lista (por fecha de creación en vez de por número), visto en pantalla y no en una prueba. +19 pruebas de base de datos, +10 unitarias, +2 E2E |
| 2026-08-08 | — | **Importación de boletas desde CSV y JSON** (BR-N12, D-081). Módulo `src/features/tickets/import/` montado sobre la carga masiva existente, con parser de CSV propio (170 líneas, sin dependencia nueva). Migración `0019`: dos funciones pequeñas. +I-042 (**la `0019` está solo en local**). Cambio aditivo en código compartido: `RowValidation` gana un campo `problem` para poder contar «repetida en el archivo» aparte de «ya existe en la rifa» sin comparar textos. +26 unitarias, +14 de base de datos, +8 E2E |
| 2026-08-08 | — | Selección múltiple y acciones masivas (BR-B01..BR-B08, D-082 a D-085), migración `0020` aplicada en local y producción. Totales: 286 unitarias, 371 de base de datos y 212 E2E. I-043 resuelto; I-044 e I-045 documentados |
| 2026-08-09 | — | Auditoría documental de continuidad Claude Code ↔ Codex (D-086), sin cambios funcionales. Registra I-046–I-053, corrige el estado vivo a 20 migraciones y preserva los snapshots históricos |
| 2026-08-09 | — | Clientes opcionales por fila en CSV/JSON, con celular obligatorio, identidad conservadora y persistencia atómica (BR-N12, D-087). `0021` se aplicó después al proyecto real con respaldo y sondas remotas; I-054 resuelto y despliegue coordinado desde `main` |
| 2026-08-10 | — | «Mis boletas» del vendedor sin filtro ni columna de rifa (D-088), publicado y verificado en producción. +I-055 (**una prueba de la importación afirmaba sobre la boleta equivocada** y tumbaba el CI de forma aleatoria), **resuelto el mismo día**; no era un defecto del producto. Sin migraciones ni cambios de esquema |
| 2026-08-14 | — | **Corregir a un integrante mientras su invitación sigue pendiente** (BR-E14..BR-E19, D-097), migración `0026` **aplicada también al proyecto real el 2026-08-14**, con respaldo previo y verificación por catálogo y por comportamiento. `profiles.activated_at` distingue «invitación pendiente» de «cuenta activa», y el vendedor padre puede corregir datos, cambiar el correo y eliminar el alta antes de que la persona entre. +I-057 (**`test:db` falla de forma intermitente al repetirlo sobre la misma base**, preexistente) y +I-058 (el CSV de vendedores conserva «Activo»). El primer diseño de «activada» —un trigger sobre `auth.users.encrypted_password`— resultó falso y lo destapó una prueba: GoTrue escribe un hash aleatorio al verificar el enlace de invitación, así que abrir el correo activaba la cuenta; corregido antes de integrar. Totales: 309 unitarias, 457 de base de datos y 246 E2E |
